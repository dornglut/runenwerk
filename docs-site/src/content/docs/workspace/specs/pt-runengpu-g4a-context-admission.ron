// Decision-complete implementation specification for RunenGPU G4A.
// Planning issue #182 owns this specification. Rust implementation requires a separate active issue.

PhaseImplementationSpec(
    phase_id: "PT-RUNENGPU-G4A",
    title: "Context and Adapter/Device Admission",
    lifecycle_state: "implemented-pending-review",
    implementation_authorization_status: "implemented-in-dornglut-runenwerk-issue-186-pending-review-and-merge",
    owner: "RunenGPU contract owner",
    source_baseline: "62c3949d31a7c03f1f554f8108120d9767139123",
    planning_issue: "dornglut/runenwerk#182",
    implementation_issue: "dornglut/runenwerk#186",
    parent_issue: "dornglut/runenwerk#167",
    completed_predecessor_issue: "dornglut/runenwerk#177",
    completed_predecessor_pull_request: "dornglut/runenwerk#181",
    completed_predecessor_merge: "39d6fe65a334502bdfba0b1a2ce3b365099fcf28",
    target_repository_after_internal_conformance: "dornglut/runen-gpu",

    authority_docs: [
        "AGENTS.md",
        "TESTING.md",
        "ARCHITECTURE.md",
        "docs-site/src/content/docs/architecture/repository-family-architecture.md",
        "docs-site/src/content/docs/adr/accepted/0015-separate-gpu-execution-from-rendering.md",
        "docs-site/src/content/docs/design/active/runengpu-architecture-design.md",
        "docs-site/src/content/docs/design/active/runengpu-g4-context-program-realization-design.md",
        "docs-site/src/content/docs/design/active/runenrender-internal-decomposition-execution-plan.md",
        "docs-site/src/content/docs/reports/investigations/runengpu-g4-context-program-realization-investigation.md",
        "docs-site/src/content/docs/reports/closeouts/pt-runengpu-g3-implementation-closeout.md",
        "docs-site/src/content/docs/workspace/planning/roadmap.md",
        "docs-site/src/content/docs/workspace/planning/active-work.md",
    ],

    goal: "Create one future-transferable, async, headless-first GpuContext admission authority with opaque context and device-generation identity, normalized backend and portability facts, deterministic requirement admission and degradation, private WGPU instance/adapter/device/queue ownership, and structured diagnostics; migrate current context consumers without implementing reusable surfaces or G5 execution.",

    implementation_record: [
        "Implemented pending review in issue #186 from accepted base 62c3949d31a7c03f1f554f8108120d9767139123.",
        "Public G4A types are GpuContext, GpuContextDescriptor, GpuAdapterFacts, GpuAdmittedDeviceFacts, GpuContextAdmissionReport, GpuCandidateDisposition, GpuContextId, GpuDeviceGeneration, and GpuContextAffinity.",
        "GpuContext::request is the async headless terminal; private backend/wgpu/{device_request,state,current_host}.rs owns WGPU Instance, Adapter, Device, and Queue.",
        "The exact temporary G7 deletion inventory is crate-private GpuContext::request_for_current_host, which makes current-host compatibility a mandatory private selection constraint independent of public Presentation requirements and returns the initial temporary surface only to the current Runenwerk host owner.",
        "GpuContext exposes no surface creation, capability, configuration, acquisition, resize, or presentation operation; its sole crate-private current-host surface bridge is opaque, borrowed, and limited to create/capabilities/configure while WgpuCtx owns surfaces, configurations, resize policy, acquisition, and surface errors.",
        "The exact temporary G4C deletion inventory is crate-private GpuContext::current_render_device_queue, a borrowing Device/Queue-only loan used by the current renderer.",
        "engine/src/plugins/render/backend/device.rs and RenderBackendTimingCapabilities are deleted; timing reads admitted TimestampQuery facts instead.",
    ],

    dependency_direction: [
        "Runenwerk host policy -> RunenGPU GpuContext admission -> private WGPU backend.",
        "RunenRender and independent consumers use normalized context facts and never receive raw WGPU Device or Queue authority.",
        "The future-transferable G4A boundary contains no renderer, ECS, UI, SDF, Winit, application, product, filesystem, or surface semantics.",
    ],

    phase_ownership: [
        "G2/G3 remain authoritative for normalized logical requirements, resources, access, operations, and prepared work.",
        "G4A owns context request, adapter/device admission, context identity, current device generation, normalized backend/adapter/device facts, requirement admission, degradation, and private WGPU context containment.",
        "G4B owns programs, entry points, interfaces, bindings, layouts, specialization, and pipeline descriptors.",
        "G4C owns generation-bound resource/program/layout/bind-group/pipeline realization and renderer cutover.",
        "G5 owns command encoding, submission, progress, pressure, completion, mapping, readback, cancellation, and retirement.",
        "G7 owns reusable surface creation/configuration/acquisition/presentation, device replacement, loss, and reconstruction facts.",
        "Runenwerk retains blocking terminals, Winit/window policy, one crate-private temporary host-compatibility request envelope, and product recovery.",
    ],

    public_contracts: [
        "Opaque nonzero process-local GpuContextId with no persistence or wire promise.",
        "Opaque nonzero GpuDeviceGeneration beginning at 1 for each newly admitted context.",
        "GpuContextAffinity containing exact context identity and device generation.",
        "GpuContextDescriptor with diagnostic label/provenance, normalized power preference, software/fallback policy, backend allowlist, portability policy, merged requirements, and no raw or temporary host/backend value.",
        "GpuContext::request(descriptor) as the reusable asynchronous headless-first constructor.",
        "GpuBackendFamily with Vulkan, Metal, Direct3D12, OpenGl, BrowserWebGpu, and UnknownBackend only.",
        "GpuAdapterClass with Discrete, Integrated, Virtual, Cpu, Other, and Unknown.",
        "Software status and observed fallback evidence are independent normalized adapter facts rather than backend-family or request-policy state: forced WGPU fallback requests are ConfirmedFallback; ordinary native enumeration and non-forced backend-selected candidates are Unknown unless the pinned backend supplies explicit non-fallback proof. Forbid rejects Unknown and ConfirmedFallback evidence; no public-policy narrowing is implied by enumeration.",
        "GpuPortabilityClass with PortableBaseline, PortableWithDeclaredExtensions, BackendSpecialized, and Unsupported.",
        "GpuAdapterFacts, GpuAdapterLimits, GpuDeviceLimits, GpuWorkloadBudget, and GpuAdmittedDeviceFacts separate adapter support, actual created-device facts, and RunenGPU workload policy.",
        "Opaque nonzero process-local GpuCandidateId values are allocated once rather than recreated from per-request ordinals. Each disclosed retry token binds the normalized descriptor, complete normalized candidate-set snapshot, and one exact normalized candidate while excluding diagnostic names and driver facts. Unknown or stale tokens, changed snapshots, and no-longer-admissible targets fail structurally; genuinely indistinguishable candidates remain ambiguous rather than falling back to enumeration order. Tokens are neither persistent nor hardware identity promises.",
        "GpuContextAdmissionReport, candidate reports, typed requirement dispositions, degradation records, structured request errors, and selection-rank evidence; rejected candidate reports retain their normalized adapter facts and rejection category without nesting another disposition collection, and all complete dispositions use one canonical normalized order independent of enumeration.",
    ],

    context_identity_semantics: [
        "Allocate GpuContextId only after complete adapter/device admission succeeds; failed requests publish no public identity.",
        "Start GpuDeviceGeneration at nonzero value 1.",
        "G4A never silently changes generation and does not implement device replacement.",
        "Every future G4C realization records GpuContextAffinity and rejects unequal context or generation before backend access.",
        "Logical G2/G3 handles remain backend-neutral and are not mutated to carry a context merely by declaration.",
        "Context IDs and generations are runtime correlation values only and must not be persisted as stable authority.",
    ],

    normalized_facts: [
        "Normalize backend family, adapter class, independent software/fallback status, supported features, enabled features, adapter limits, actual device limits, workload budget, format capabilities, and operation-relevant alignments.",
        "GpuAdapterLimits record pre-creation adapter support, GpuDeviceLimits record device.limits() after creation, and GpuWorkloadBudget records caller-constrained G4A policy; none is an alias for either other fact.",
        "A software adapter retains its actual Vulkan, Direct3D12, OpenGl, or other backend family; software is never modeled as a backend API family.",
        "Retain adapter name, vendor/device IDs, and sanitized AdapterInfo.driver/driver_info only as optional diagnostics and compatibility facts supplied by the backend; name and driver facts never participate in ranking, retry identity, or persistent identity.",
        "Unknown backend, adapter, or software/fallback facts remain explicit Unknown values and are never guessed into a known category.",
        "Initial alignments cover uniform/storage dynamic offsets, copy buffer offsets, copy row pitch, query-resolve destination offsets, and texture block constraints where applicable.",
        "Supported, requested, granted, degraded, and rejected facts remain distinct in reports.",
    ],

    requirement_admission_algorithm: [
        "Normalize and validate all requirements before backend access.",
        "Merge equal requirements; combine minimum limits by maximum requested minimum and maximum constraints by minimum permitted maximum; reject contradictions.",
        "Order evaluation and diagnostics by typed requirement identity, independent of caller insertion order.",
        "Reject a candidate when backend family, adapter class, or independently observed software/fallback status violates explicit policy; no backend family, name, driver, vendor, device, or adapter class is guessed into fallback evidence.",
        "Mandatory requirements reject a candidate when unsupported.",
        "Preferred requirements may degrade only through one explicit G4A compatibility mapping: Compute, RenderPipeline, Copy, IndirectDraw, StorageTexture, and DepthAttachment use SelectAlternativeWork; TimestampQuery uses DisableInstrumentation; Presentation uses ContinueWithoutFeature. Descriptor validation rejects every other feature/fallback pair as InvalidDegradation before backend access.",
        "Enable only features required by accepted mandatory/preferred requirements plus backend-mandatory features; do not opportunistically enable unrelated features.",
        "Derive a workload budget from one fixed G4A baseline (64 KiB uniform binding, 128 MiB storage binding, 1 color attachment, 8 vertex buffers, and 16 bindings per group), raise only explicitly required minima, apply permitted maxima, and reject any candidate that cannot support the resulting request; never request adapter maxima merely because they are available.",
        "Bind each admitted request to an explicit complete pinned-WGPU profile (modern portable, downlevel, browser/WebGPU, or downlevel WebGL2), reject an unsupported profile before device creation, request all requestable selected alignments, and verify actual device limits and alignments before publication.",
        "Evaluate format roles and alignments before device publication.",
        "Derive portability class from admitted requirements and contained backend specialization.",
        "For native Allow, enumerate all WGPU adapters with Unknown fallback evidence, retain every complete disposition in canonical normalized order, and rank accepted candidates before device request by observed fallback evidence, power preference, portability class, adapter class, backend preference, vendor, and device facts. Native Forbid rejects Unknown or ConfirmedFallback evidence unless a separate owner decision explicitly narrows that public policy. Diagnostic names and driver facts are never selection rank inputs.",
        "If multiple accepted candidates remain indistinguishable after every permitted normalized tie-break, return AmbiguousAdapterSelection with opaque process-local candidate IDs. An exact retry validates its disclosed token against the unchanged normalized request and candidate-set snapshot, then selects only its uniquely matching admitted candidate; stale/unknown tokens fail structurally and genuinely indistinguishable matches remain ambiguous. Never use backend enumeration order.",
        "Native Require uses the private pinned-WGPU forced-fallback request path, normalizes its result as ConfirmedFallback, preserves current-host compatibility evidence, and reports BackendSelectedCandidate; backend-selected-only platforms run the same path with non-forced candidates normalized as Unknown and without claiming deterministic hardware selection.",
        "Bound every public diagnostic string and formatted request error by UTF-8 byte length without splitting a character; formatted errors retain deterministic rejection-category counts and one safe representative rejection reason where space permits.",
    ],

    async_and_terminal_rules: [
        "The library constructor returns a future and never calls pollster::block_on.",
        "A Runenwerk binary/host adapter may provide a blocking terminal outside the future-transferable boundary.",
        "Cancellation before successful return publishes no partially admitted context.",
        "No consumer callback is invoked while an internal lock is held.",
        "G4A binds only context-request completion; G5 later binds progress for accepted GPU work.",
        "Native and web use the same structured terminal result categories.",
    ],

    raw_wgpu_containment: [
        "WGPU Instance, Adapter, Device, Queue, Features, Limits, TextureFormat, resources, shader/layout/pipeline objects, Surface, and SurfaceConfiguration remain private backend types.",
        "No public field, Deref, AsRef<Device>, broad callback, universal native-handle escape hatch, or render re-export may expose raw WGPU authority.",
        "A private WgpuContextState may own instance, adapter, device, and queue and may be reached only by crate-private backend terminals.",
        "Public methods return normalized facts, structured results, or opaque generation-bound handles.",
    ],

    temporary_host_compatibility: [
        "Permit exactly one Runenwerk-owned crate-private request envelope pairing a public GpuContextDescriptor with opaque compatibility evidence solely to preserve current surface-compatible adapter selection while G7 is pending.",
        "Do not place the compatibility value in the public descriptor, public builder, standalone API, or reusable context identity.",
        "Current-host compatibility is mandatory for every candidate on that envelope regardless of public Presentation requirements; incompatible candidates are rejected with TemporaryHostCompatibilityFailure before ranking, while headless admission has no host constraint and an explicit mandatory Presentation requirement still rejects headless admission.",
        "The initial terminal may create and return one temporary surface solely as adapter-selection evidence. GpuContext does not configure, acquire, present, resize, or own a reusable surface in G4A.",
        "Permit one opaque crate-private G7 migration bridge borrowed from GpuContext with only create, capabilities, and configure operations. It owns no surface, exposes no raw WGPU authority, and is deleted with the current-host terminal when G7 owns reusable surface authority.",
        "WgpuCtx owns the surface collection, each Surface and SurfaceConfiguration, configuration policy, resize, acquisition, and SurfaceError handling.",
        "Native backend-handle reconnection matches normalized selected facts plus compatibility evidence; it never uses first-match enumeration order and reports zero or multiple matching backend candidates structurally.",
        "Keep headless context admission independent and the default architecture.",
        "Record the compatibility path as G7 deletion inventory and guard against a second path.",
    ],

    target_source_areas: [
        "engine/src/plugins/gpu/api/context/{identity,descriptor,diagnostics,facts,admission,selection}.rs with one owner per concern and context.rs as its public module root",
        "engine/src/plugins/gpu/backend/wgpu/{adapter_mapping,device_request,state,current_host}.rs with mod.rs as its private module root",
        "engine/src/plugins/gpu/mod.rs complete-subtree guards",
        "engine/src/plugins/render/backend/device.rs migration and deletion of replaced authority",
        "engine/src/plugins/render/backend/wgpu_ctx.rs migration and reduction to explicitly temporary G7 evidence where required",
        "engine/src/plugins/render/renderer/mod.rs removal of public raw Device/Queue reach-through",
        "current context consumers in apps/runenwerk_draw, apps/runenwerk_editor, engine/examples, engine/benches, and engine/tests",
    ],

    migration_rules: [
        "Introduce the new context owner before changing consumers.",
        "Migrate headless-capable consumers first, then the current window-compatible host adapter.",
        "Replace RenderBackendTimingCapabilities with normalized feature admission rather than forwarding it.",
        "Remove public Gfx.ctx.device and Gfx.ctx.queue authority; do not add aliases or accessor methods returning raw WGPU.",
        "Do not move surface configuration, acquisition, presentation, loss, or reconstruction into G4A.",
        "Delete replaced instance/adapter/device/queue creation authority in the same accepted slice.",
    ],

    deterministic_tests: [
        "Context identity allocation is nonzero and unique after successful synthetic admission.",
        "Device generation begins at 1 and exact affinity equality rejects foreign context and stale generation.",
        "Requirement normalization and reports are invariant to caller order.",
        "Contradictory limits fail before backend request.",
        "Mandatory feature/limit/format/alignment misses reject candidates.",
        "Preferred misses require one explicit valid feature/fallback mapping and emit one typed record; exhaustive descriptor tests reject every invalid pair as InvalidDegradation.",
        "Unrelated supported features are not enabled.",
        "Synthetic candidate ranking follows the accepted deterministic ordering, preserves rejected adapter facts and selection evidence, canonically orders complete accepted and rejected dispositions across forward, reverse, and rotated inputs, binds retry tokens to an unchanged normalized snapshot rather than ordinals, rejects stale/unknown tokens, and keeps genuinely indistinguishable retry targets ambiguous.",
        "Portability classes derive exactly from admitted extensions, backend specialization, and unknown-backend unsupported outcomes; baseline-only policy rejects every non-baseline outcome.",
        "Empty and constrained requests derive the same workload budget independently of adapter maxima or candidate enumeration order.",
        "Every device-request profile is complete and predictable, unsupported profiles reject during pure admission, and actual device facts are verified against the admitted contract before context publication.",
        "Known backend mappings, adapter classes, software/fallback facts, sanitized driver facts, and explicit unknown mappings are exhaustive for the pinned WGPU version.",
        "Public dependency/source guards reject raw WGPU, Winit, renderer, ECS, application, product, and temporary host-compatibility vocabulary.",
    ],

    environment_tests: [
        "Request a headless native adapter/device where the environment provides one.",
        "The non-ignored native environment test accepts only real successful admission or typed NoAdapterAvailable; rejected candidates, ambiguity, adapter/device failures, and every other unexpected category fail the test with bounded diagnostic detail.",
        "Prove actual enabled features, actual device facts, workload budget, admission contract, and complete dispositions match the admitted report; successful headless admission does not claim Presentation.",
        "Exercise the crate-private temporary host-compatible selection only as an explicitly environment-dependent smoke.",
        "Compile and run WebGPU coverage in the supported platform matrix when available.",
    ],

    deletion_guards: [
        "No public WgpuCtx device or queue field.",
        "No reusable RunenGPU constructor taking winit::window::Window.",
        "No public raw WGPU type in the future-transferable boundary.",
        "No Software variant in GpuBackendFamily; software/fallback remains an adapter fact.",
        "No public host-compatibility value and no second crate-private compatibility path.",
        "No second instance/adapter/device/queue owner under render.",
        "No compatibility alias forwarding deleted renderer context types.",
        "No generated prompt, task database, truth certificate, execution lock, or temporary authoring workflow.",
    ],

    exclusions: [
        "No G4B program/interface types.",
        "No G4C resource/program/pipeline realization cutover beyond stubs strictly required by context identity tests.",
        "No G5 encoding, submission, progress, completion, readback, cancellation, retirement, or shutdown behavior.",
        "No G7 public surface, presentation, device replacement, loss, or reconstruction behavior.",
        "No RunenRender semantics or package extraction.",
        "No dependency, manifest, lockfile, workflow, stable persistence, wire format, or new ADR unless a genuine stop condition is reached.",
    ],

    validation: [
        "Focused deterministic context/admission tests and dependency guards.",
        "Focused environment-dependent headless WGPU tests with explicit unsupported outcomes.",
        "cargo validate",
        "git diff --check",
        "CI=true pnpm --dir docs-site build",
        "Repository-owned exact-head GitHub Actions and Documentation Build.",
        "All tracked files remain below 131072 raw bytes.",
    ],

    acceptance: [
        "One async headless-first GpuContext request authority exists.",
        "Context and device-generation semantics are explicit and tested.",
        "Requirement admission, degradation, candidate ranking, and ambiguity are deterministic and structured.",
        "Normalized backend/adapter/software/portability/feature/limit/format/alignment facts replace renderer timing-only capability authority.",
        "Raw WGPU instance/adapter/device/queue ownership is private and no public reach-through remains.",
        "Current hosts are migrated through one crate-private compatibility path and its bounded G7 bridge without changing the public descriptor or transferring reusable surface ownership from G7.",
        "No G4B, G4C, G5, G7, RunenRender, or extraction scope entered the diff.",
        "Exact-head validation, independent review, migration census, and deletion guards pass.",
    ],
)
