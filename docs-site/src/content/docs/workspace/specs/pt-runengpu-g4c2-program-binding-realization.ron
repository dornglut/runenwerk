// Decision-complete implementation specification for RunenGPU G4C2.
// G4C2 starts only after accepted G4C1, accepted prerequisite corrections,
// and an explicitly activated child issue on exact accepted main.

PhaseImplementationSpec(
    phase_id: "PT-RUNENGPU-G4C2",
    title: "Program, Layout, and Binding Realization",
    lifecycle_state: "planned-blocked",
    implementation_authorization_status: "blocked-until-accepted-prerequisites-and-explicit-g4c2-issue",
    owner: "RunenGPU program and binding realization owner",
    source_baseline: "accepted-main-to-be-recorded-by-g4c2-issue",
    planning_issue: "dornglut/runenwerk#209",
    implementation_issue: "dornglut/runenwerk#213",
    umbrella_issue: "dornglut/runenwerk#188",
    parent_issue: "dornglut/runenwerk#167",
    blocking_predecessor: "accepted PT-RUNENGPU-G4C1 plus accepted G4C2 readiness corrections",
    successor: "PT-RUNENGPU-G4C3",

    authority_docs: [
        "AGENTS.md",
        "TESTING.md",
        "ARCHITECTURE.md",
        "engine/Cargo.toml",
        "Cargo.lock",
        "docs-site/src/content/docs/design/active/runengpu-shader-authoring-artifact-boundary.md",
        "docs-site/src/content/docs/design/active/runengpu-g4b-contracts-g4c-delivery-design.md",
        "docs-site/src/content/docs/design/active/runengpu-g4c2-presentation-surface-binding-boundary.md",
        "docs-site/src/content/docs/workspace/specs/pt-runengpu-g4b-program-interface-layout.ron",
        "docs-site/src/content/docs/workspace/specs/pt-runengpu-g4c-wgpu-realization-cutover.ron",
        "docs-site/src/content/docs/workspace/specs/pt-runengpu-g4c1-resource-realization.ron",
    ],

    goal: "Create one private context/device-generation-bound WGPU authority for canonical WGSL modules, mandatory version-matched Naga evidence, WGPU module acceptance, normalized observed shader-stage IO, bind-group layouts, pipeline layouts, typed bind groups, bounded authoritative registries and optional discardable caches; supersede and delete the G4C1 object-reference resource bridge with one narrowly scoped pipeline-successor bridge that carries only proven residual resource terminals plus G4C2 program/layout/bind-group terminals; remove G4C2-owned creation from the separate operation loan; and avoid realizing compute/render pipelines or performing final renderer cutover.",

    dependency_direction: [
        "Accepted G4B admitted source records, entry points, resource interfaces, stage-IO comparison vocabulary, and layouts -> G4C2 private WGSL and binding realization.",
        "Accepted G4C1 opaque resources -> G4C2 runtime binding compatibility and typed bind groups.",
        "The existing engine-private direct Naga dependency parses and validates canonical WGSL for normalized evidence; the original canonical WGSL text remains the only WGPU shader source submitted for backend realization.",
        "G4C2 deletes CurrentRenderResourceBridge and replaces it with CurrentRenderPipelineBridge, which carries only the proven residual G4C1 resource terminals that G4C2 does not itself migrate.",
        "CurrentRenderDeviceQueue remains a separately classified crate-private backend-operation loan, not an object-reference bridge. G4C2 removes ShaderModule, BindGroupLayout, PipelineLayout, and BindGroup creation from it while retaining only later-phase operation classes.",
        "Accepted G4C2 program records, observed stage-IO signatures, layouts, and bind groups -> G4C3 through opaque handles and CurrentRenderPipelineBridge, which also carries only the program/layout/bind-group terminals still required by current pipeline creation and unchanged G5 encoding.",
    ],

    pinned_parser_baseline: [
        "Accepted planning base engine/Cargo.toml directly pins wgpu 27.0.1 and naga 27.0.3 with the wgsl-in feature.",
        "The G4C2 implementation issue must re-resolve exact accepted main and record cargo tree evidence for the direct Naga parser/validator version and every WGPU-internal Naga version.",
        "Proceed only when the direct evidence path is version-compatible with the pinned WGPU implementation; if Cargo resolves different incompatible Naga major/minor authorities or WGPU changes its WGSL validation baseline, stop for the smallest explicit dependency-baseline decision.",
        "Reuse the existing direct dependency when alignment is proven; do not add a second parser, reflection crate, compiler frontend, or parallel shader-source path.",
        "For the accepted Naga 27.0.3 baseline, instantiate the evidence validator with exactly naga::valid::ValidationFlags::all() and naga::valid::Capabilities::default().",
        "The Naga 27.0.3 default capability set is the initial portable source profile. Do not add a capability merely because a broader GpuCapabilityFeature is admitted: the current RunenGPU capability vocabulary does not provide a lossless one-to-one authority for Naga extension capabilities.",
        "Capabilities::all(), manual permissive supersets, and backend-feature-to-Naga guesses are forbidden as acceptance evidence.",
        "Any Naga capability beyond Capabilities::default() requires a separately accepted RunenGPU capability/profile contract with a deterministic mapping before G4C2 may enable it.",
        "Define G4C2_NAGA_VALIDATION_PROFILE_REVISION = 1 for the accepted Naga 27.0.3 + ValidationFlags::all() + Capabilities::default() + normalization/agreement behavior; increment it only when one of those compatibility semantics changes under accepted authority.",
        "Define G4C2_WGPU_REALIZATION_COMPATIBILITY_REVISION = 1 for the accepted WGPU 27.0.1 WGSL/scoped-creation lowering behavior; increment it only when backend realization compatibility semantics change under accepted authority.",
        "No public RunenGPU type exposes naga::Module, naga::valid::ModuleInfo, handles, arenas, bindings, validation flags, capabilities, errors, or source spans.",
        "Naga types remain confined to one private G4C2 WGSL-evidence module and are normalized immediately into RunenGPU-owned facts and bounded diagnostics.",
    ],

    wgsl_evidence_pipeline: [
        "Parse the exact canonical WGSL bytes with the pinned direct naga::front::wgsl path; do not parse a rewritten, formatted, preprocessed, or backend-generated source variant.",
        "Validate the parsed module with exactly the accepted versioned G4C2 Naga validation profile.",
        "Normalize entry points, resource bindings, access, type/shape/cardinality, applicable layout, vertex-input, fragment-output, builtin, override, and requirement facts from the validated module and module-info structures.",
        "Run G4B's resource-interface agreement before private WGPU module publication and retain observed stage-IO signatures for G4C3.",
        "Separately submit the original canonical WGSL text through WGPU's WGSL shader-source path; do not submit naga::Module through a Naga shader-source variant and do not create a second runtime source kind.",
        "WGPU 27.0.1 backend creation acceptance is asynchronous: surround each publishing G4C2 WGPU creation with nested Internal, OutOfMemory, and Validation error scopes, create the object, pop the scopes in reverse stack order, and await every scope before authoritative publication.",
        "A shader-module Validation scope error after successful direct Naga evidence is ShaderValidationPathMismatch and retains bounded evidence from both paths; it is never silently resolved in favor of one validator.",
        "A layout or bind-group Validation scope error is UnexpectedBackendProgramOrBindingValidationRejection; an OutOfMemory scope error is BackendResourceExhaustion; an Internal error or observed device loss is ContextOrDeviceUnavailableOrLost.",
        "No shader module, bind-group layout, pipeline layout, or bind group record is published before its scoped backend acceptance futures resolve successfully.",
        "RunenGPU realization terminals that require this scoped backend proof are async. RunenGPU does not own an executor and must not call pollster::block_on, spin an event loop, or hide synchronous waiting inside the framework/backend boundary.",
        "A Runenwerk product/integration boundary may choose how to drive an async realization future for current synchronous callers; that policy is outside RunenGPU authority and cannot become bridge, cache, registry, or backend semantics.",
        "Publish one G4C2 program realization only when direct Naga parse/validation/evidence normalization succeeds, explicit resource-interface agreement succeeds, and WGPU accepts the same canonical WGSL text.",
        "The direct Naga parse is evidence and diagnostics machinery, not public source identity, runtime module authority, a persisted IR, or a replacement for WGPU backend creation.",
    ],

    realized_handle_contracts: [
        "Opaque Clone non-Copy handles cover realized programs/modules, bind-group layouts, pipeline layouts, and bind groups.",
        "Every handle retains GpuContextAffinity and complete admitted descriptor or request identity for typed inspection.",
        "Program handles retain normalized observed resource-interface facts and observed vertex-input/fragment-output signatures from the pinned direct Naga evidence path.",
        "Observed facts are validation evidence only and cannot create resource declarations, vertex-buffer state, or color-target policy.",
        "Handles expose no public raw WGPU object, Naga IR, backend module output, pointer, persistence identity, or wire identity.",
        "Bind-group handles retain exact layout identity and ordered realized binding values.",
    ],

    wgsl_realization_contract: [
        "Consume only canonical WGSL admitted through the accepted G4B source registry.",
        "Reject unknown, evicted-without-live-reference, foreign-owner, or source-revision-conflicting records before parser or backend creation.",
        "Run the complete WGSL evidence pipeline before publishing the private WGPU shader module realization on a registry miss.",
        "Normalize and retain observed vertex entry-point input locations/classes and fragment entry-point output locations/classes for later G4C3 pipeline compatibility.",
        "Naga IR, module-info, and backend-native outputs remain private ephemeral implementation facts and never become public identity, cache, persistence, replay, or wire formats.",
        "Deterministic parser, validator, or agreement incompatibility rejects before backend creation. Unexpected backend validation after complete admission, resource allocation exhaustion/OOM, and unavailable/lost context/device remain distinct structured outcomes; none publishes an authoritative realization or cache entry, and backend text is diagnostic only.",
    ],

    mandatory_reflection_agreement: [
        "Normalize observed entry-point names and stages, group/binding identities, stage visibility, resource classes, access, dimensions, sample/storage format facts, array cardinality, applicable host-layout facts, vertex-input locations/classes, and fragment-output locations/classes into G4B-owned vocabulary.",
        "Use the single G4B resource-interface comparison algorithm to prove observed resource facts agree with the explicit authoritative declarations; observed statically-used stages must be a subset of declared visibility, while declared visibility may be a strict accessibility superset.",
        "Reject missing, additional, renumbered, stage-mismatched, type-mismatched, visibility-mismatched, shape-mismatched, or layout-incompatible resource facts as ProgramInterfaceMismatch.",
        "Retain normalized stage-IO signatures with the program record; do not compare them to vertex buffers or color targets until G4C3 receives a complete render pipeline descriptor.",
        "Evidence may improve source-span diagnostics but may not mutate, infer, renumber, add, or remove authoritative declarations or pipeline state.",
        "ProgramInterfaceMismatch is terminal for that realization request and cannot fall back to inferred WGPU layouts.",
    ],

    observed_stage_io_contract: [
        "Observed vertex input signatures are keyed by exact vertex entry point and contain unique shader locations plus normalized scalar/vector classes.",
        "Observed fragment output signatures are keyed by exact fragment entry point and contain unique color locations plus normalized scalar/vector classes.",
        "Builtins are represented separately from user locations and cannot be mistaken for vertex attributes or color targets.",
        "Duplicate or unsupported observed locations/classes reject program realization with a structured WGSL validation error.",
        "Observed signatures are retained realization evidence and participate in the accepted record's compatibility facts; they are not required to locate an already-published record because they do not exist before first realization.",
        "Observed signatures are not public source identity, renderer semantics, persistence authority, or a replacement for explicit render pipeline state.",
    ],

    layout_realization_contract: [
        "Realize bind-group layouts only from accepted complete GpuBindGroupLayoutDescriptor values.",
        "Realize pipeline layouts only from accepted ordered unique group-layout descriptors.",
        "Do not use WGPU automatic layout inference or shader evidence as layout authority.",
        "Validate relevant admitted features, limits, dynamic-offset constraints, array cardinality, storage texture formats, and sampler/texture compatibility before backend creation.",
        "When a group contains any binding array, enforce pinned WGPU group restrictions: no buffer in that group may use a dynamic offset and no buffer in that group may be uniform.",
        "Equal complete request descriptors may reuse one authoritative layout realization within one context/generation.",
    ],

    typed_bind_group_contract: [
        "A bind-group request names one accepted realized layout and supplies ordered typed binding values.",
        "Each value references accepted G4C1 resource handles or complete inline sampler semantics where the accepted API permits it.",
        "Validate exact resource kind, usage, range, minimum size, dynamic-offset policy and alignment, texture view/format/sample/multisample facts, storage access, sampler class, array cardinality, and context/device-generation affinity before backend creation.",
        "Reject unknown, duplicate, missing, extra, foreign, stale, or incompatible values with structured binding diagnostics.",
        "Backend bind-group creation is async, scoped, transactional, and publishes no partial authoritative entry.",
        "Every current bind-group consumer that G4C2 owns uses typed G4C1 handles directly, so CurrentRenderResourceBridge is deleted before G4C2 acceptance while only separately proven residual resource terminals move into CurrentRenderPipelineBridge.",
    ],

    validation_order: [
        "Validate admitted source/request structure and exact context affinity.",
        "Reject stale device generation.",
        "Reject an unavailable or lost context/device before authoritative lookup; an accepted record never becomes usable evidence for a dead device generation merely because its request key still matches.",
        "Construct the complete pre-realization typed request key and perform authoritative registry lookup using full equality after hashing.",
        "On an exact accepted-record hit in the same healthy context/generation, return that realization without rerunning direct Naga and without creating another WGPU object.",
        "On a program miss, run direct Naga parse, full validation with the accepted validation profile, normalized evidence extraction, and expected G4B program-interface/entry-point agreement on the exact canonical WGSL.",
        "On a program miss, submit the byte-identical canonical WGSL to WGPU under scoped async backend error capture and require all scopes to resolve successfully.",
        "On layout and bind-group misses, validate complete descriptors and typed runtime values against admitted device facts, accepted interfaces, and G4C1 realizations before scoped backend creation.",
        "Re-check context/device health after scoped backend completion and immediately before publication so a concurrent device-loss fact cannot publish a newly unusable record.",
        "Publish private program/layout/bind-group registry entries transactionally only after every deterministic and backend check succeeds.",
    ],

    registry_invariants: [
        "Separate private registries own program/module, bind-group-layout, pipeline-layout, bind-group, and cache-diagnostic records.",
        "Each registry instance is scoped to exactly one GpuContextId and GpuDeviceGeneration.",
        "GpuProgramBindingRealizationPolicy owns one total nonzero authoritative record bound shared by program/module, bind-group-layout, pipeline-layout, and bind-group records; no per-kind quota is implied.",
        "DEFAULT_MAX_PROGRAM_BINDING_REALIZATION_RECORDS is 16384, matching the established G4C1 default operational scale until measurement justifies a separately accepted change.",
        "Introduce GpuRealizationPolicies as the context-level composition of GpuResourceRealizationPolicy and GpuProgramBindingRealizationPolicy. GpuContext::request uses default policies; GpuContext::request_with_realization_policies accepts an explicit composition without changing adapter selection, device-admission identity, or retry identity.",
        "Migrate callers and delete request_with_resource_realization_policy rather than retaining overlapping context-construction APIs; the unified policy composition is the one explicit override path after G4C2.",
        "Before RegistryCapacityExceeded, deterministically sweep every authoritative G4C2 record retained only by its own registry. A record retained by any live opaque handle or authoritative dependent record is live and cannot be reclaimed.",
        "Registry reclamation removes lookup authority and may release ordinary private WGPU object ownership; it is not GPU completion evidence, does not authorize early physical-retirement semantics, and does not move G5 lifecycle ownership.",
        "Cache eviction is never authoritative-record reclamation and may not make a live handle unknown.",
        "If the total record bound remains full after the registry-only sweep, fail with RegistryCapacityExceeded; never silently evict a live authoritative record.",
        "Each program entry retains its complete pre-realization request key plus the admitted source record, direct-evidence compatibility revision, complete explicit resource interface, normalized observed resource facts, observed stage-IO signatures, WGPU acceptance fact, and bounded diagnostics/provenance.",
        "Each layout and bind-group entry retains its complete typed request key/descriptor and exact dependency identities plus bounded backend acceptance facts.",
        "Equal duplicate requests return the same realization record; changed requests do not replace accepted identities.",
        "Hash collisions cannot authorize reuse because full typed equality follows hashing.",
        "No consumer callback executes under registry locks and no async backend wait occurs while a registry lock is held.",
        "No failure publishes partial state.",
    ],

    cache_key_contracts: [
        "GpuProgramRealizationRequestKey is computable before Naga/WGPU work and includes context identity, device generation, source owner/key/revision, full canonical WGSL bytes and deterministic digest, declared entry points, complete expected resource interface, admitted requirements, G4C2_NAGA_VALIDATION_PROFILE_REVISION, and G4C2_WGPU_REALIZATION_COMPATIBILITY_REVISION.",
        "Full canonical WGSL byte equality remains authoritative; the digest only accelerates lookup/diagnostics and can never authorize reuse by itself.",
        "Normalized observed resource facts, observed stage-IO signatures, and WGPU acceptance evidence belong to the realized program record; they are deliberately excluded from the pre-realization lookup key because they do not exist until a miss is realized.",
        "Bind-group-layout request keys include context affinity, complete group descriptor, relevant admitted features/limits, and internal descriptor compatibility revision.",
        "Pipeline-layout request keys include context affinity, ordered complete group-layout identities, relevant admitted features/limits, and internal descriptor compatibility revision.",
        "Bind-group request keys include context affinity, complete layout identity, ordered typed values, complete G4C1 realization identities/ranges/generations, and relevant admitted facts.",
        "Labels, filesystem paths, authoring compiler handles, source maps, raw Naga handles, renderer IDs, WGPU object addresses, backend error text, and naked hashes are excluded.",
    ],

    cache_behavior: [
        "Authoritative registry lookup uses complete pre-realization request keys. An exact live registry match in a healthy context/generation returns the accepted realization directly and records Hit without rerunning evidence/backend creation.",
        "An absent authoritative record records Miss and follows the complete ordinary realization path.",
        "Optional derived acceleration caches are in-memory, discardable, reconstructable, context/generation-scoped, and non-authoritative. A stale/incompatible derived entry records Rejected, is discarded or quarantined, and falls back to authoritative lookup/ordinary realization.",
        "A cache never bypasses affinity, generation, current context/device health, full-key equality, or authoritative-record liveness checks.",
        "Resource-interface or Naga/WGPU validation-path disagreement is a semantic rejection, not a cache miss or fallback.",
        "Unexpected backend validation, backend resource exhaustion/OOM, and unavailable/lost context/device emit distinct structured outcomes and publish no authoritative entry; RealizationFailed may aggregate telemetry but is not their semantic classification.",
        "No persisted module, layout, bind-group, Naga IR, module-info, observed signature, backend output, or cross-process handle format is authorized.",
    ],

    successor_migration_bridge: [
        "Delete CurrentRenderResourceBridge before publishing G4C2 acceptance; it has zero remaining source references and its still-required access is represented only by the successor's proven residual terminals.",
        "Permit exactly one crate-private module named CurrentRenderPipelineBridge after G4C2 acceptance.",
        "The bridge exists only because audited current pipeline-creation and unchanged G5 encoding operations still require the proven residual G4C1 resources and G4C2 program/layout/bind-group objects until G4C3 supersedes it.",
        "The bridge accepts validated opaque G4C1 resource and G4C2 program, bind-group-layout, pipeline-layout, and bind-group handles and exposes only narrowly typed owner-local terminals for exact audited current pipeline-creation and encoding consumers; it exposes no public accessor, Deref, AsRef, reusable raw handle, generic raw-WGPU callback, or arbitrary consumer closure.",
        "The bridge may lend borrowed resource, shader-module, layout, and bind-group references only for lexical use through those purpose-typed terminals; borrowed values cannot escape, be stored, cloned into ownership, cached, returned, persisted, or treated as resource/program/layout/pipeline identity.",
        "A terminal may support its unchanged later-phase operation, but the bridge never performs or owns pipeline realization, command encoding, uploads, submission, async driving, or any other operation.",
        "The bridge owns validated access only; it does not validate stage IO and owns no resource, source, interface, layout, pipeline descriptor, cache, execution, progress, completion, or lifecycle truth.",
        "Bridge lookup repeats exact context/device-generation, current context/device-health, and handle-kind validation and invokes no external callback under registry locks.",
        "Source guards permit exactly this named object-reference bridge and its audited current terminals and reject a second pipeline bridge, the deleted resource bridge, or broad raw-WGPU object-reference reach-through outside the separately bounded CurrentRenderDeviceQueue operation loan.",
        "G4C3 must delete CurrentRenderPipelineBridge before publishing G4C3 acceptance, migrating terminals it owns and carrying only proven residual resource/bind-group terminals plus newly realized G4C3 pipeline terminals into its single execution successor bridge.",
    ],

    current_render_device_queue_cutover: [
        "CurrentRenderDeviceQueue is a separate crate-private backend-operation loan, not CurrentRenderPipelineBridge, not a second object-reference bridge, and not a generic callback, native-interop, Deref, or AsRef API.",
        "At G4C2 acceptance, its source-guarded exact call sites have zero ShaderModule, BindGroupLayout, PipelineLayout, or BindGroup creation operations, in addition to the G4C1 resource-creation removals.",
        "The remaining operation classes and exact call sites are monotonically shrinking G5 evidence. G4C3 removes pipeline creation; G5 migrates encoding/upload/submission/copy/map/readback users and deletes the loan.",
    ],

    current_consumer_migration: [
        "Repeat the exact-current-main census for shader-module, binding-layout, pipeline-layout, bind-group, runtime-binding, parameter-byte, sidecar, renderer setup, material, UI, example, bench, and test consumers.",
        "Repeat the dependency census for every direct Naga and WGPU use and record the exact module that owns private WGSL evidence.",
        "Migrate explicit program/interface/layout/bind-group realization through the async G4C2 owner while retaining filesystem/watch/reload/LKG policy in Runenwerk and semantic shader meaning in RunenRender or other consumers.",
        "Do not introduce blocking, executor, or event-loop policy in RunenGPU. If current synchronous Runenwerk code must drive a G4C2 future, locate that choice at the narrowest audited Runenwerk product/integration boundary and source-guard it as non-framework policy.",
        "Migrate CurrentRenderResourceBridge terminals that G4C2 owns to typed G4C2 bind-group realization, delete that predecessor bridge, and record the only residual resource terminals that the successor may retain.",
        "Migrate canonical WGSL/module/layout/bind-group creation out of CurrentRenderDeviceQueue. Keep its independently audited later-phase operation classes separate from object-reference bridge terminals.",
        "Convert unavoidable current renderer pipeline creation and unchanged G5 encoding access to CurrentRenderPipelineBridge terminals rather than leaving renderer-owned resource/module/layout/bind-group registries or a parallel G4C2 realization path.",
        "Classify render-pipeline stage-IO comparison, compute/render pipeline realization, and final renderer cutover explicitly for G4C3 rather than changing them here.",
        "Preserve existing G5 command encoding and G7 surface behavior as migration evidence and ownership; an unchanged G5 operation may lexically consume a successor-terminal borrow without moving execution into G4C2.",
    ],

    delete_after_migration: [
        "CurrentRenderResourceBridge and every source reference to it; each still-required access must instead be an exact CurrentRenderPipelineBridge terminal or a G4C2-owned realization path.",
        "Renderer-owned WGPU shader-module, bind-group-layout, pipeline-layout, and bind-group registries or caches fully replaced by G4C2.",
        "The superseded GpuContext::request_with_resource_realization_policy context-construction path after all callers migrate to GpuRealizationPolicies.",
        "Automatic or inferred WGPU layout authority.",
        "Program/interface/layout/bind-group keys based only on strings, paths, TypeId, renderer IDs, labels, or naked hashes.",
        "Parallel WGSL module, Naga evidence, or runtime binding realization paths outside the RunenGPU owner.",
        "Broad raw resource/module/layout/bind-group access outside the one accepted CurrentRenderPipelineBridge at the G4C2 boundary.",
        "G4C2-owned program, resource-interface, observed-stage-IO, layout, binding, evidence, and cache truth in the temporary G3 execution sidecar.",
    ],

    structured_failures: [
        "ForeignContext",
        "StaleDeviceGeneration",
        "UnknownAdmittedSource",
        "SourceRevisionConflict",
        "NagaDependencyBaselineMismatch",
        "WgslParseOrValidationFailed",
        "ShaderValidationPathMismatch",
        "ProgramInterfaceMismatch",
        "ObservedStageIoInvalid",
        "UnknownLayout",
        "LayoutDescriptorInvalid",
        "RuntimeBindingIncompatible",
        "BindingValueMismatch",
        "RegistryCapacityExceeded",
        "CacheRejected",
        "UnexpectedBackendProgramOrBindingValidationRejection",
        "BackendResourceExhaustion",
        "ContextOrDeviceUnavailableOrLost",
        "CurrentRenderPipelineBridgeViolation",
    ],

    deterministic_tests: [
        "Foreign-context, stale-generation, and unavailable/lost-context rejection precede registry-hit success, parser access, and backend access.",
        "A device-loss fact observed after first realization prevents a later equal request from succeeding through an otherwise matching registry/cache hit.",
        "The exact Naga 27.0.3 validation profile is ValidationFlags::all() plus Capabilities::default(); guards reject Capabilities::all(), manual capability supersets, and guessed broad-feature mappings.",
        "G4C2_NAGA_VALIDATION_PROFILE_REVISION and G4C2_WGPU_REALIZATION_COMPATIBILITY_REVISION are both exactly 1 at the accepted initial baseline and participate in request identity.",
        "Version and configuration facts for the direct Naga evidence path are explicit and participate in compatibility identity.",
        "Synthetic normalized WGSL facts exercise every mandatory agreement and exact mismatch category.",
        "Observed vertex/fragment signatures normalize independently of arena/parser enumeration order and reject duplicate or unsupported user locations/classes.",
        "Evidence cannot add, remove, renumber, or mutate authoritative declarations or pipeline state.",
        "Program request keys are fully computable before realization, include full canonical WGSL byte equality plus digest, and exclude post-realization observed/WGPU acceptance evidence; equal accepted hits do not invoke Naga or WGPU again.",
        "Complete layout/bind-group request keys are insertion-order-independent and include every pre-realization correctness field.",
        "Hash collisions never authorize reuse; full typed equality is mandatory.",
        "Equal duplicate realization reuses one record; changed request identity creates or selects a distinct record rather than replacing accepted authority.",
        "No authoritative publication occurs before all matching WGPU error scopes resolve; shader-module validation disagreement, layout/bind-group validation rejection, OOM, and internal/device-loss outcomes remain distinct.",
        "A device-loss fact observed during scoped creation prevents publication even if all ordinary creation calls returned objects.",
        "Program/binding registry policy has one total nonzero 16384-record default, no per-kind quota, deterministic registry-only reclamation, no live-handle eviction, and RegistryCapacityExceeded after an unsuccessful sweep.",
        "Registry reclamation and cache eviction cannot be interpreted as GPU completion or G5 retirement authority.",
        "No G4C2 registry lock is held while awaiting a WGPU error-scope future.",
        "Typed binding values reject wrong kind, range, size, alignment, format, sample class, access, array cardinality, foreign context, and stale generation.",
        "CurrentRenderResourceBridge has zero remaining source references at G4C2 acceptance.",
        "CurrentRenderPipelineBridge rejects foreign, stale, unavailable/lost, wrong-kind, escape, storage, caching, return, identity, and bridge-owned pipeline/encoding/upload/submission/async-driving attempts and has only audited purpose-typed current terminals.",
        "Labels, provenance, source maps, authoring paths, raw Naga handles, and backend diagnostic text do not split equal request identities.",
        "Source-string guards are limited to topology/deletion/containment invariants such as forbidden raw creation sites, bridge uniqueness, and deleted symbols; behavioral correctness is proven by typed and behavior tests.",
        "Source and dependency guards reject public Naga/WGPU authority, a second evidence path, renderer/ECS/application/product ownership, broad access, a second object-reference bridge, hidden RunenGPU blocking/executor ownership, and every G4C2-owned creation class through CurrentRenderDeviceQueue.",
    ],

    environment_tests: [
        "Parse and validate representative canonical WGSL compute and render programs through the exact direct Naga dependency and accepted validation profile resolved for the implementation head.",
        "Submit the byte-identical canonical WGSL strings to WGPU under scoped async validation/OOM/internal capture and require successful private module creation only after all scope futures resolve and current device health is re-checked.",
        "Prove explicit resource declarations agree with normalized direct-Naga evidence.",
        "Retain real normalized vertex-input and fragment-output signatures for the representative render program.",
        "Reject one direct-Naga parse/validation failure, one resource-interface mismatch, and one bounded Naga/WGPU validation-path disagreement fixture where the pinned APIs permit deterministic construction.",
        "Realize representative bind-group and pipeline layouts and one typed bind group using accepted G4C1 resources with scoped backend completion before publication.",
        "Prove an exact second healthy-context program request returns the same accepted record without a second Naga parse or WGPU module creation.",
        "Exercise explicit small program/binding registry capacity, registry-only reclamation, live-handle retention, and RegistryCapacityExceeded.",
        "Exercise one current representative compute or render pipeline-creation terminal and one unchanged G5 encoding terminal through CurrentRenderPipelineBridge without implementing G4C3 pipeline authority or moving G5 ownership.",
        "Prove authoritative-record reuse within one context/generation and isolation across another context/generation.",
    ],

    source_and_dependency_guards: [
        "The direct naga dependency remains private to the G4C2 WGSL-evidence owner; no naga type crosses engine/src/plugins/gpu public modules.",
        "Canonical WGSL text remains the only WGPU shader-source path; ShaderSource::Naga or equivalent IR submission is forbidden.",
        "No second WGSL parser, reflection library, shader compiler, or alternate source frontend is introduced.",
        "Cargo tree evidence proves the accepted direct evidence version and WGPU validation baseline are compatible at the exact implementation head.",
        "The accepted Naga validation-profile constants are centralized in one private evidence owner and source guards reject Capabilities::all or a second validator configuration.",
        "No RunenGPU source path contains pollster::block_on, executor ownership, event-loop pumping, or a synchronous wrapper around a G4C2 publishing realization future.",
        "No automatic/inferred WGPU layouts, public raw WGPU, or broad private callback path is introduced.",
    ],

    exclusions: [
        "No compute or render pipeline realization under RunenGPU authority and no comparison of stage IO against a G4B render pipeline descriptor.",
        "No final renderer-wide realization/cache cutover beyond module/layout/bind-group authority fully replaced here.",
        "No object-reference raw-WGPU bridge other than CurrentRenderPipelineBridge after CurrentRenderResourceBridge deletion. CurrentRenderDeviceQueue remains separately bounded as an operation loan with no G4C1/G4C2 creation classes and no generic callback escape hatch.",
        "No new Naga/WGPU dependency or feature change when exact-current-main already proves the accepted direct baseline; any required dependency-baseline change stops for separate authorization.",
        "No additional Naga capability without separately accepted RunenGPU capability/profile mapping authority.",
        "No authoring compiler, WESL, Slang, HLSL, GLSL, SPIR-V, source discovery, watch, reload scheduling, or last-known-good implementation.",
        "No G5 execution, submission, progress, readback, cancellation, retirement, delayed destruction, or shutdown redesign; an unchanged G5 operation may only use a lexical successor-terminal borrow.",
        "No G7 surface, presentation, device replacement, loss, or reconstruction implementation.",
        "No RunenRender semantics, package extraction, stable format, compatibility facade, generated workflow state, inferred layout authority, or public raw-WGPU escape hatch.",
    ],

    validation: [
        "Focused deterministic and environment-dependent program/layout/binding/evidence proof.",
        "Complete current-consumer, direct-dependency, validation-path, predecessor-bridge deletion, successor-bridge call-site, CurrentRenderDeviceQueue operation-class/call-site, async-driving boundary, realization-policy, and authority-deletion census.",
        "cargo tree -p engine --locked evidence for wgpu and every naga version",
        "cargo validate",
        "git diff --check",
        "CI=true pnpm --dir docs-site build",
        "Repository-owned exact-head CI and Documentation Build.",
        "Independent complete-diff review with no unresolved blocking finding.",
        "All tracked files remain below 131072 raw bytes.",
    ],

    acceptance: [
        "One private context/generation-bound async WGSL module, layout, and bind-group realization owner exists.",
        "The exact current dependency graph proves one compatible direct Naga evidence path for the pinned WGPU baseline or records a separately accepted dependency correction.",
        "The exact Naga validation profile is ValidationFlags::all() plus Capabilities::default(), with G4C2_NAGA_VALIDATION_PROFILE_REVISION = 1 and no guessed extension capability mapping.",
        "The initial WGPU realization compatibility revision is G4C2_WGPU_REALIZATION_COMPATIBILITY_REVISION = 1 for the accepted WGPU 27.0.1 scoped-creation behavior.",
        "Every newly published program has direct Naga parse/validation evidence, explicit G4B resource agreement, retained stage-IO signatures, scoped WGPU acceptance of the same canonical WGSL text, and a final healthy-device check before publication.",
        "Every exact live program request hit in a healthy context returns its accepted record without rerunning Naga or WGPU creation; request identity contains only complete pre-realization facts, includes full canonical WGSL byte equality, and full typed equality follows hashing.",
        "Typed layouts and bind groups use accepted G4C1 resource handles, complete compatibility validation, scoped async backend acceptance, and current device-health checks before publication.",
        "GpuRealizationPolicies supplies one explicit context-construction override path for both G4C1 resource and G4C2 program/binding record policies; the superseded resource-only request path is deleted after migration.",
        "The G4C2 authoritative record default is one shared nonzero 16384-record bound with deterministic registry-only reclamation, no live authority eviction, and no G5 completion/retirement semantics.",
        "Optional caches are derived and non-authoritative; authoritative registry/request keys and outcomes are correctness-complete and structured.",
        "CurrentRenderResourceBridge and every replaced renderer-owned module/layout/bind-group authority are deleted without aliases or a parallel path; each still-required resource terminal is either G4C2-owned or an exact CurrentRenderPipelineBridge terminal.",
        "Exactly one CurrentRenderPipelineBridge remains for G4C3 replacement and deletion, carrying only proven residual resource terminals plus current program/layout/bind-group terminals; no broader raw reach-through remains.",
        "CurrentRenderDeviceQueue remains separately non-public and source-guarded, with zero G4C1 resource and G4C2 module/layout/bind-group creation call sites; it is not counted as an object-reference bridge.",
        "RunenGPU owns no executor or hidden synchronous wait for G4C2 publication. Any current blocking/async-driving policy is explicitly outside the RunenGPU boundary.",
        "Compute/render pipelines and final stage-IO compatibility remain blocked for G4C3.",
        "Exact-head validation and independent review pass, then G4C3 may be activated only after accepted-main verification of G4C2.",
    ],
)
