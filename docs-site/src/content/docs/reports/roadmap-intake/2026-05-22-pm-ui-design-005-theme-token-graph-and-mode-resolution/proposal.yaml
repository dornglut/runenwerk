version: 1
idea: PM-UI-DESIGN-005 Theme Token Graph And Mode Resolution
source_path: docs-site/src/content/docs/design/active/ui-designer-interface-lab-platform-design.md
owner: domain/ui/ui_theme
created_at: '2026-05-22'
governance_notes:
- Run architecture governance review before implementation.
- Confirm Clean Architecture dependency direction and DDD owner.
- Record ADR only if the decision changes durable ownership, dependency direction, or cross-domain contracts.
- Architecture governance for PM-UI-DESIGN-005 keeps generic token graph and deterministic resolution in `domain/ui/ui_theme`,
  Canonical UI IR token references in `domain/ui/ui_definition`, editor/workbench package adapters in `domain/editor/editor_definition`,
  and app-hosted Theme Designer surfaces in `apps/runenwerk_editor`.
- No new ADR is required for the first bounded generic token graph implementation row.
open_questions:
- None for ready-next planning; implementation still requires task production:plan, promotion preflight, focused tests, and
  closeout evidence before PM-UI-DESIGN-005 can be completed.
item:
  id: WR-049
  title: PM-UI-DESIGN-005 Theme Token Graph And Mode Resolution
  diagram_title: PM-UI-DESIGN-005 Theme Token Graph And Mode Resolution
  alias: UITOKENS
  lane: Product workflow
  dependency_level: L2
  gate: After PM-UI-DESIGN-005 design acceptance
  planning_state: ready_next
  priority: P1
  value: 4
  blocker: 2
  tc: 3
  rr_oe: 4
  du: 3
  effort: 8
  confidence: 0.5
  expected_score: 0.9
  rice: First code-bearing UI Designer theme token graph slice after PM-002 and PM-005 design gates.
  kano: Basic
  dependencies:
  - WR-046
  write_scopes:
  - domain/ui/ui_theme
  - domain/ui/ui_definition
  - docs-site/src/content/docs/design/accepted
  validations:
  - cargo test -p ui_theme token
  - cargo test -p ui_theme
  - task docs:validate
  - task production:validate
  - task roadmap:validate
  next_evidence: Production plan and implementation contract for deterministic token graph layers, alias cycle rejection,
    mode/state precedence, accessibility override conflict diagnostics, and target-profile compatibility diagnostics.
  current_decision: Ready-next bounded implementation row for the PM-UI-DESIGN-005 generic UI theme token graph slice;
    no product code has started.
  current_call: Run task production:plan before implementation. Keep app-hosted Theme Designer UI, editor-specific package
    storage, and broad preview/runtime styling out of this first generic token row.
  first_move: Run task production:plan -- --milestone PM-UI-DESIGN-005 --roadmap WR-049 and follow the generated promotion
    or implementation contract.
  main_blocker: Needs production planning, promotion preflight, and focused implementation contract before code.
  why_not_ready: ''
  completion_quality: not_applicable
  known_quality_gaps: []
  completion_audit: ''
  diagram_call:
  - token graph layers
  - mode and state precedence
  - accessibility diagnostics
  decision_gates:
  - kind: design
    path: docs-site/src/content/docs/design/accepted/ui-designer-theme-tokens-modes-skins-and-state-variants-design.md
    required_status: accepted
    applies_to: implementation
    reason: Theme token graph implementation requires accepted deterministic layer, alias, mode, state, skin, and diagnostics rules.
  ddd_owner: domain/ui/ui_theme
  adr_requirement: No new ADR for the first generic token graph slice; ADR required before app/runtime/editor shell state,
    renderer material handles, or provider sessions become styling source truth.
  fitness_function_requirement: Deterministic layer ordering, alias cycle rejection, missing alias rejection, token family mismatch
    diagnostics, mode/state precedence, accessibility conflict diagnostics, preview-only activation rejection, and target-profile
    compatibility tests.
  ownership_mode: Complicated-subsystem UI theme owner with stream-aligned editor app consumption.
edges: []
