[package]
name = "godot_chunking_addon"
version = "0.1.0"
edition = "2024"
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
chunking = { path = "../../domain/chunking" }
godot = "0.5.0"
spatial = { path = "../../domain/spatial" }
