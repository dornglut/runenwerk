# Render Mesh Material Production Evidence Artifacts

This folder stores raw or reproducible artifact references for WR-069 renderer
mesh/material production evidence. Human-readable reports live under
`docs-site/src/content/docs/reports/benchmarks/render`.

The initial WR-069 implementation records deterministic artifact reference
paths here; future hardware runs can add raw benchmark output next to this
README without changing the renderer inspection contract.
