# Render Scale Evidence Artifacts

Raw WR-063 renderer scale benchmark and hardware-profile artifacts belong in
this folder. Keep generated Criterion output, local adapter notes, and raw
evidence captures here instead of mixing them into prose documentation.

Human-readable summaries belong under:

```text
docs-site/src/content/docs/engine/benchmarks/render-scale-evidence.md
```
