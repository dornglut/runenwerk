# Render Temporal Production Evidence Artifacts

Raw WR-072 temporal production evidence artifacts belong in this folder.
Human-readable benchmark reports live under
`docs-site/src/content/docs/reports/benchmarks/render`.

The current portable evidence path records deterministic summary placeholders
and Criterion output references. Future hardware runs can add backend-specific
raw output next to this README without changing the public renderer API.
