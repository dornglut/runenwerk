[package]
name = "spatial_index"
version = "0.1.1"
edition = "2024"
publish = false

[dependencies]
spatial = { path = "../spatial" }
spatial_index_core = { package = "spatial_index", path = "../../../../spatial_streaming/crates/spatial_index" }
