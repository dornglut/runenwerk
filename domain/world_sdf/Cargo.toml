[package]
name = "world_sdf"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { version = "1.0.228", features = ["derive"] }
spatial = { path = "../spatial" }
world_ops = { path = "../world_ops" }
postcard = { version = "1.1.3", features = ["alloc"] }
