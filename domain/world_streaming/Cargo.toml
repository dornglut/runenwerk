[package]
name = "world_streaming"
version = "0.1.1"
edition = "2024"
publish = false

[dependencies]
chunking = { path = "../chunking" }
spatial = { path = "../spatial" }

[dev-dependencies]
chunking = { path = "../chunking" }
spatial = { path = "../spatial" }
