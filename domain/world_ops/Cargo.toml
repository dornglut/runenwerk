[package]
name = "world_ops"
version = "0.1.0"
edition = "2024"
publish = false
license.workspace = true

[dependencies]
serde = { version = "1.0.228", features = ["derive"] }
spatial = { path = "../spatial" }
