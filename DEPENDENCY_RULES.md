# Dependency Rules

Canonical dependency and clean-cutover rules live at:

```text
docs-site/src/content/docs/guidelines/dependency-rules.md
```

The stable repository-family direction is:

```text
RunenRender -> RunenGPU
framework repositories -/-> Runenwerk
```

Runenwerk owns cross-framework adapters and product integration. See the canonical document for layer, framework, adapter, versioning, and extraction rules.
