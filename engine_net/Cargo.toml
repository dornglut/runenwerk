[package]
name = "engine_net"
version = "0.1.0"
edition = "2024"

[dependencies]
ecs = { path = "../ecs" }
postcard = { version = "1.1.3", features = ["alloc"] }
serde = { version = "1.0.228", features = ["derive"] }
